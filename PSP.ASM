		.Model 	Small
		.Stack 	100H
		.data
TABLA 	DB 89H,12H,0AAH,0BH,3FH,8EH,12H,0BBH,0CCH,0DDH,9DH,45H
		DB 56H,3EH,02H,66H,0EEH,0ABH,77H,99H,00H,1CH,0B3H,0F1H
		DB 89H,0E6H,71H,81H,90H,0BCH,0DDH,0E3H,4FH,27H,0C9H,03H
		DB 89H,0B5H,0F5H,55H,9EH,67H,90H,80H,70H,60H,50H,40H,30H
SALIDA 	DW 40H DUP(?)
OP1		DB 00H
OP2		DB 00H
CONT	DB 02H
		.code
		MOV AX,SEG TABLA
		MOV DS,AX
;PSP .ICC
		MOV BX,0081H
		MOV SI,OFFSET OP1
L0:		MOV AL,DS:[BX]
		CMP AL,0DH
		JE L2
		CMP AL,"/"
		JE L1
L3:		INC BX
		JMP L0
L1:		INC BX
		MOV AL,DS:[BX]
		CMP AL,"1"
		JB L3
		CMP AL,"6"
		JA L3
		SUB AL,30H
		MOV DS:[SI],AL
		INC SI
		DEC CONT
		JNZ L3
L2:		MOV AL,OP1
		CMP AL,OP2
		JE FIN1
		CMP OP1,00H
		JE FIN1
		CMP OP2,00H
		JE FIN2
		MOV SI,OFFSET TABLA
		MOV CONT,40H
		MOV BX,0040H
		MOV AL,OP1
		MUL BL
		ADD AX,SI
		MOV DI,AX
		MOV BX,0040H
		MOV AL,OP2
		MUL BL
		ADD SI,AX
		MOV BX,OFFSET SALIDA
L4:		MOV AX,0000H
		MOV DX,0000H
		MOV AL,DS:[DI]
		MOV DL,DS:[SI]
		ADD AX,DX
		MOV DS:[BX],AX
		INC DI
		INC SI
		ADD BX,0002H
		DEC CONT
		JNZ L4
		MOV AX,4C00H	
		INT 21H
FIN:	MOV AX,4C01H
		INT 21H
		END			